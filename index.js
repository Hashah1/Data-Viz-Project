// Create SVG Canvas
const w = 800
const h = 500
const svg = d3.select("svg")
    .attr("width", w)
    .attr("height", h)
    .attr("align", "center")
    .style("border", "1px solid black")

// Extract Data
async function getData() {
    // const urbanData = await d3.csv()
    // const ruralData = await d3.csv
}
// Create Bars

// Axis, scales